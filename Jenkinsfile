pipeline {
    agent any
    stages {
        stage('jenkins test') {
            steps {
		sh '''echo "Checking work directory on build stage"'
                      pwd
                '''
            }
        }
    }
}
